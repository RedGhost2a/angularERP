import { MetreLenghtWidthHeightStrategy } from './metre-lenght-width-height-strategy';

describe('MetreLenghtWidthHeight', () => {
  it('should create an instance', () => {
    // expect(new MetreLenghtWidthHeightStrategy()).toBeTruthy();
  });
});
